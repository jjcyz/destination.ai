{
  "name": "destination-ai",
  "version": "1.0.0",
  "description": "AI-powered destination routing system",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm run build",
    "dev": "cd frontend && npm run dev",
    "install": "cd frontend && npm install"
  },
  "workspaces": [
    "frontend"
  ]
}
